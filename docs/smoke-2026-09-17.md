# MCP smoke — 2026-09-17

Recorded against `develop` after PR #41 (evidence-safe itinerary validation). Product code already on `develop`; `main` untouched. Issues #34–#40 closed on develop, not main.

## 1. `search_dates` — LHR→BKK round-trip calendar

Args:

- `origin`: LHR
- `destination`: BKK
- `trip`: round-trip
- `nights`: 14
- `bags`: 1
- `nearby`: true
- smoke date context: 2026-09-17

Key outputs:

- calendar coverage: 31/31 cells
- cheapest owned cell: **412 GBP** on **2026-11-17**

## 2. `search_hidden_city` — MAD→JFK with EUR keep

Args:

- `origin`: MAD
- `destination`: JFK
- `currency`: EUR

Key outputs:

- Skiplagged cards are USD
- result: `currency_mismatch` (owned USD quote stamped)
- not silent `no_results`
- Viajante does not invent FX / EUR conversion

## 3. `validate_itinerary` — incomplete evidence

Args:

- itinerary / offers missing owned provenance for a feasibility claim

Key outputs:

- never `feasible=true` without owned provenance
- incomplete evidence → unknown / infeasible (evidence-safe path from PR #41)
- aligns with closed issues #34–#40 on `develop`
